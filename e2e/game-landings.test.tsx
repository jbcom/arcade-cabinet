import { describe, test } from "vitest";

describe("game cartridge landing captures", () => {
  test.skip("all games have been extracted to standalone repos", () => {
    // Each extracted game owns its own landing screenshot coverage
    // in its upstream repo under `arcade-cabinet/<slug>`.
  });
});
